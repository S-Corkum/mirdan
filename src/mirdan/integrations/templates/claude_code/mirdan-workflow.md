---
description: "Mirdan quality workflow pointer"
---

# mirdan Quality Workflow

> Auto-generated by mirdan. Regenerate with `mirdan init --upgrade`.

Quality is enforced by `.claude/rules/` + PostToolUse/Stop hooks (deterministic, no
model tokens) and the validation gate below.

## Recommended Quality Workflow

`enhance_prompt` is optional by default and **recommended** before security-sensitive,
multi-file, or new-library work. `validate_code_quality` after writing remains mandatory.

## Skills

- `/plan` — grounded, Haiku-proof implementation plan (research-first, anchored edits)
- `/plan-review`, `/plan-verify` — review / mechanically verify a plan
- `/code` — quality-orchestrated implementation with execution discipline

## Tools (call when useful)

| Tool | Use |
|------|-----|
| `enhance_prompt` | optional quality framing for non-trivial/security work |
| `validate_code_quality` | mandatory gate after writing code |
| `validate_quick` | fast security check (used by the PostToolUse hook) |
| `get_quality_standards` | language/framework rules |
| `scan_conventions` / `scan_dependencies` | discover patterns / vuln scan |

Violations come with fix suggestions — apply with `mirdan fix <file>` or inline.

## Project

- **Language**: {{language}}
- **Frameworks**: {{frameworks}}
